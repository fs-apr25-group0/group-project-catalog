import close from '../../../images/icons/close.svg';
import '../Icon.scss';

export const CloseIcon = () => {
  return (
    <img
      src={close}
      alt="Close"
      className="icon"
    />
  );
};
